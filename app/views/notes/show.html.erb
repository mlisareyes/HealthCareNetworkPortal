<b>Note:</b> <%= @note.content %></p>
<p><b>Noted By:</b> Dr. <%= @note.user.name %></p>

<%= button_to 'Edit Note', edit_note_path(@note), method: :get %>
<%= button_to 'Delete Note', note_path(@note), method: :delete, data: { confirm: "Are you sure you want to delete this note?" } %>
