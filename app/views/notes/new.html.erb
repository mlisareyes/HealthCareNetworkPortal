<h1>New Note</h1>

<%= form_for(@note) do |f| %>
  <%= f.label :note %>
  <br>
    <br>
  <%= f.text_area :content %>
  <br>
    <br>
    <%= f.hidden_field :patient_id %>
  <%= f.submit 'Post Note' %>
<% end %>
